# Simple Docker with Nginx + PhpFpm  (with composer)


# Build

docker-compose -f ./docker/docker-compose.yml build

# Run
docker-compose -f ./docker/docker-compose.yml up


# Stop
docker-compose -f ./docker/docker-compose.yml down
